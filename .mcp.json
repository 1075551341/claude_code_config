{
  "$schema": "https://github.com/modelcontextprotocol/spec/blob/main/schema/schema.json",
  "_comment": "MCP v9.2 常驻 5 服务器 | 权威源 | ops/optional-dev 见 mcp-configs/*.json 按需 merge | Cursor 侧见 docs/CURSOR_MCP_PROFILE.md",
  "mcpServers": {
    "codegraph": {
      "_description": "代码知识图谱 MCP（R17 首选）。场景：符号探索、调用链、影响面。",
      "command": "npx",
      "args": ["-y", "@colbymchenry/codegraph", "serve", "--mcp"]
    },
    "crawl": {
      "_description": "Firecrawl 官方 MCP。场景：网页抓取、结构化提取、爬虫与搜索（L2/L3 调研）。",
      "command": "powershell",
      "args": [
        "-NoProfile",
        "-ExecutionPolicy",
        "Bypass",
        "-File",
        "C:\\Users\\DELL\\.claude\\scripts\\firecrawl-mcp.ps1"
      ]
    },
    "git": {
      "_description": "Git 版本库直接操作。场景：提交历史、diff、blame、本地分支。",
      "command": "uvx",
      "args": ["mcp-server-git", "--repository", "D:\\apdms"]
    },
    "fs": {
      "_description": "文件系统操作。场景：跨路径读写、配置仓库维护。",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "D:\\apdms", "C:\\Users\\DELL\\.claude"]
    },
    "time": {
      "_description": "时间与时区工具。场景：获取当前时间、时区转换（配合禁 new Date() 规范）。",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-time"]
    }
  }
}
