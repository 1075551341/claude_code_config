# v10.10 配置驱动整合优化 — 执行留痕

> 日期: 2026-07-31 | 状态: **已完成** | 前置: v10.9.0（30 仓库调研 + 使用率审计 + 同步 v18 已完成）
> 依据任务书: `d:\download\AI助手全局配置-优化提示词.md`（v2 优化版）三视角规划 → 计划批准 → 本文件为执行记录

## 目标

1. 修复双端同步链路，确保 Claude Code 与各编辑器（cursor/qoder-cn/trae-cn/codearts）同步后正常使用
2. 「配置驱动模型执行（非模型自由发挥）」：门控 hooks 双端可用 + 可测量
3. 合并冗余重复配置为单一 SSOT，版本/口径/文档三对齐

## 决策确认记录（用户 2026-07-31 授权）

| #   | 决策                 | 结论                               | 依据                                                                                  |
| --- | -------------------- | ---------------------------------- | ------------------------------------------------------------------------------------- |
| D1  | codebase-memory 处置 | ✅ **永久禁用**（用户确认）        | 启用后疯狂消耗 CPU/内存（全盘索引）；文档统一"禁用+根因"，codegraph 全权替代          |
| D2  | MCP 常驻分层         | ✅ fetch 常驻、crawl 按需          | fetch 高频已修复；crawl 为 L2/L3 调研场景                                             |
| D3  | 7 个零触发技能处置   | ✅ 保持观察一轮                    | 均为 L3 按需，常驻成本=0                                                              |
| D4  | CLAUDE.md 瘦身幅度   | ✅ 仅删纯冗余，门控 SSOT 一字不动  | 强制力由 hooks 承载                                                                   |
| D5  | 触发日志增强         | ✅ 实施                            | 「配置驱动」可测量证明                                                                |
| D6  | ~/.cursor 根符号链接 | ✅ **保留**（执行调整）            | 7 个链接均指向真源（无内容漂移），check.ps1 SYNC_FILES 依赖，删除收益为零且验证不充分 |
| D7  | Cursor 规则通道      | ✅ **plugin 永久方案**（用户确认） | `~/.cursor/rules` 实测不生效，不做其他通道尝试                                        |

## 执行摘要（T1-T16 全部完成）

### 阶段 0 安全

- **T1 密钥外置**：settings.json `ANTHROPIC_AUTH_TOKEN` → `${ANTHROPIC_AUTH_TOKEN}`（值已入用户环境变量）；.gitignore 追加 settings.json/settings.local.json；`git rm --cached`（本地保留）
- **T2 基线**：git 状态 + plugin hash（389B57...）/sync-stamp（v10.8.0）记录

### 阶段 1 同步链路修复

- **T3 sync.ps1 v18.2**：新增 `-Scope rules|indexes|all` + `-Force`（对齐 sync_runner.py 契约，修复 Guard 自动同步必失败）；Sync-File/plugin 变更检测（hash 跳过）；结尾图谱刷新 `--force` 移除（尊重 90s debounce）
- **T4 重跑 sync -All**：plugin 00-CLAUDE-ROUTER.mdc hash 与真源一致；.sync-stamp → v10.10.0；`~/.cursor/rules` 保持空
- **T5 Guard 去重**：hooks.json 移除 postToolUse 双注册（sync_on_edit/knowledge_graph_sync_hook 保留 afterFileEdit 单点）；重部署 18 hooks（guard_version 1.1.7）
- **T6 check.ps1**：S4b 断言改为 plugin 通道（三件套 hash 与真源一致 + `~/.cursor/rules` 期望空）；$EDITORS 补 qoder-cn/trae-cn/codearts；NATIVE_RULES/editorDir 对齐；**结果 Pass 41 / Fail 0**

### 阶段 2 一致性

- **T7 版本串**：MANIFEST/README/CLAUDE/SPEC/hooks README/SYNC_GUIDE(v18.2)/scripts README 全部对齐 v10.10.0；skills 44→45；hooks 15→16；global_skills_max 46→45
- **T8 cbm 口径**：SPEC/MANIFEST/CORE.md/plugin 模板统一"永久禁用（全盘索引爆 CPU/内存）→ codegraph 全权替代"
- **T9 文档 plugin 永久通道**：SYNC_GUIDE 模式 A/映射表、CURSOR_EDITOR_SETUP 一致性清单改写
- **T10 遗留**：删除 sync-mode.json；MANIFEST context_monitor owner 对齐运行态；~/.cursor 根链接保留（D6）

### 阶段 3 合并与瘦身

- **T11 重复合并**：CLAUDE.md -9 行（R17 收敛为 CORE SSOT 指针、场景映射表删双份、/deer-flow 行删、@RTK.md → 指针、插件数字 → 指针、cbm 升级表述删）；ROUTER cbm 双处合并；门控文本完整（GATE_INTACT=YES）
- **T12 MCP 分层**：crawl → optional-dev.json（按需）；.mcp.json 常驻 5（codegraph+fetch+git+fs+time）；MANIFEST 注释回写
- **T13 触发日志**：stop-session-summary.py 追加 skill/agent 真实触发日志（transcript 解析 → `~/.claude/logs/skill-triggers.jsonl`），fail-open + 显式报告（R16）

### 阶段 4 验证与留痕

- **T14 门控冒烟**：① P0 分类门（SessionStart）**实测注入成功**；② 完成验证门（UserPromptSubmit）**实测注入成功**（R1 证据要求）；③ 变更影响门（PreToolUse Edit）静态验证（同源 gate_messages SSOT；sandbox 限制 python 实机）；v10.7 计划 W6/W7 → 验收归档
- **T15 全量验证**：validate_config **ALL CHECKS PASSED**（V10/V18 修复；V5 对齐）；check.ps1 **Pass 41 / Warn 5 / Fail 0**（报告 logs/check-20260801.md）；audit_hooks launcher 全覆盖（2 个 330s timeout 为图谱同步设计使然）；docs/README 同步
- **T16 本文件**（决策记录 + 执行留痕）

## 复核修复（2026-08-01 完成验收复核）

- **validate_config.py 入口块恢复**：重写 V18 时误删 `if __name__ == "__main__"`（直接运行不执行检查）；已恢复并验证 `ALL CHECKS PASSED / EXIT=0`
- **check.ps1 部署口径对齐**：非 cursor 编辑器 SYNC_FILES 检查收窄为仅 CLAUDE.md（路由入口经 rules/ 内 00-CLAUDE-ROUTER 检查，S3 router 已覆盖）→ 消除 qoder-cn/trae-cn/codearts 误报
- **plans/ 目录补齐**（gitignore 运行时目录）；SYNC_GUIDE 章节版本标注 v17.0/v18.0 → v18.2
- **复核后指标**：check.ps1 **Pass 45 / Warn 1（Docker 未装）/ Fail 0 / Health 98**；validate_config ALL CHECKS PASSED（9 个既有 warn）；plugin hash/版本/Guard 去重全部达标；门控文本完整

## 遗留（观察项，非阻断）

- validate_config 9 个既有 warn（V1 触发词共享×2、V9 deny 安全建议×4、V17 压缩环境变量×3）— 历史存在，未纳入本次范围
- 7 个零触发技能保持观察一轮，下一轮 usage-audit 凭 `logs/skill-triggers.jsonl` 真实数据再判
- sync.ps1 `Remove-SameBasenameVariants` 逐文件枚举未重构为目录快照（变更检测已达成等价性能目标：无变更时 O(n) hash 跳过）
- Cursor Guard 的 `knowledge_graph_sync_hook.py`/stop hook `--force` 保留（会话结束低频确定性刷新，合理设计）

## 变更文件清单

settings.json · .gitignore · scripts/sync.ps1 · scripts/check.ps1 · scripts/validate_config.py · hooks/stop-session-summary.py · hooks/session-start-bootstrap.py(版本) · templates/cursor-guard/hooks.json · templates/cursor-guard/hooks/\_lib/gate_messages.py(版本) · templates/cursor-claude-config-plugin/rules/CORE.mdc · hooks/\_lib/gate_messages.md(版本) · MANIFEST.yaml · CLAUDE.md · CLAUDE-ROUTER.mdc · SPEC.md · README.md · rules/CORE.md · rules-INDEX.md · agents-INDEX.md · skills-INDEX.md · hooks/README.md · docs/README.md · docs/SYNC_GUIDE.md · docs/CURSOR_EDITOR_SETUP.md · docs/RUNTIME_PLAYBOOK.md · docs/research/README.md · docs/research/COVERAGE.md · docs/research/30-repo-deep-research-v10.md(头部) · docs/superpowers/plans/2026-07-30-v10.7-gate-enforcement.md(归档) · scripts/README.md · .mcp.json · mcp-configs/optional-dev.json · 删除 sync-mode.json
