# v10.6.0 全局配置全量优化计划（合并版 SSOT）

> 日期: 2026-07-29 | 依据: v10.5.2 调研成果 + 30 仓库清单复核 + 访谈 Q1–Q4 | 状态: **批准执行**
> 本文档合并原 `2026-07-17-v10.5-optimization.md` 与 `2026-07-17-v10.5.1-optimization.md`（均已验收，原件已删，决策脉络保留于此）。

## 一、决策脉络（历史访谈锁定）

### v10.5（2026-07-17，已验收）

| # | 结论 |
| --- | -------------------------------------------------------------- |
| Q1 | 三波串行 |
| Q2 | 分层 delta |
| Q3 | Cursor 工具纪律：soft_block + MCP P0(codegraph+cbm) + 常驻边界 |
| Q4 | 默认不重开 v10.1；上游版本仅文档跟踪（R14） |

### v10.5.1（2026-07-17，已验收）

| # | 结论 |
| --- | --------------------------------------- |
| Q1 | 分层 delta；不推翻五柱×五阶段×三横切 |
| Q2 | 钉现状；上游仅文档「待评估」（R14） |
| Q3 | Tier-1 双源；Tier-2 gh |
| Q4 | UA **removed** |
| Q5 | cbm 场景强制；Claude L4；Cursor P0 |
| Q6 | 多编辑器 sync；修 CONTEXT/CORE/MCP 过期 |
| Q7 | 三波串行 |
| Q8 | **v10.5.1** patch |

### v10.5.2（2026-07-28，已验收）

工具调用门控优化（禁止/强制场景表、场景-工具映射 L0 常驻）。详见 `docs/diagnostic-v10.5.2.md`。

## 二、本轮访谈锁定（v10.6.0，2026-07-29）

| # | 决策点 | 结论 |
| --- | ------------- | ------------------------------------------------------------------ |
| Q1 | 版本号 | **v10.6.0**（minor：结构清理 + 索引重建） |
| Q2 | 文档清理 | **激进**：删旧 plan 两份、gsd-gaps、REPO_ANALYSIS（独有内容并入 COVERAGE）；ADR 全保留 |
| Q3 | 浅层调研卡 | **不补深**：仅修正元数据/版本漂移 |
| Q4 | hooks 治理 | **归档+正名**：stub 除名，`_optional/` → `_archive/` 非激活资产库 |
| Q5 | 零触发技能 | **全部保留观察一轮**（L3 常驻成本为零，下轮审计再判） |

## 三、目标与非目标

### 目标

1. 版本串全库对齐 v10.6.0（修 10.1/10.4.0/10.5.1 漂移）
2. 文档集"留最全面最新一次，其余删掉"：调研 SSOT + 28 卡 + COVERAGE 保留；旧 plan 合并于此；REPO_ANALYSIS 并入 COVERAGE
3. 常驻上下文瘦身：CORE.md ≤150 行，治理详情迁 GOVERNANCE.md（按需）
4. 索引补全：skills-INDEX 44 全量；修复 2 个 SKILL.md 重复 frontmatter
5. hooks 名实一致 + 零使用率审计（候选清单逐项确认）
6. OpenSpec 三处表面职责边界文档化；同步机制复验

### 非目标

- 不重构 sync.ps1 为中立真源+build 架构（现机制满足需求，R10）
- 不补深 14 张浅层调研卡；不动 plugins/、catalog/、MCP 配置、settings.json 核心
- 不升级任何依赖 major（R14）；不自动升 OpenSpec 1.6 / GSD 1.7 / codegraph 1.4 / cbm 0.9 / superpowers 6.1 / claude-mem 13.11（沿用 v10.5.1 Q2）

## 四、执行工作流（W1–W9）

| # | 工作流 | 要点 |
| --- | ------ | ---------------------------------------------------------------- |
| W1 | 版本对齐 | SPEC/README/skills-INDEX/MANIFEST/research-README → v10.6.0 |
| W2 | 文档精简 | 本文档合并旧 plan；删 gsd-gaps + REPO_ANALYSIS（并入 COVERAGE）；task-master 归档 `docs/research/archive/` |
| W3 | 索引修复 | skills-INDEX 补 7 技能；修 2 个 SKILL.md frontmatter；刷新 INDEX |
| W4 | CORE 瘦身 | ≤150 行 + 新建 rules/GOVERNANCE.md + CONTEXT/BESTPRACTICE/WORKFLOW 去重 + 路由注册 |
| W5 | hooks 治理 | 3 stub → `_deprecated/`；`_optional/` → `_archive/`；README 正名 |
| W6 | 使用率审计 | claude-mem+history → `usage-audit-v10.6.md` 候选清单（逐项确认后动配置） |
| W7 | OpenSpec | 三表面职责表（Claude commands / Cursor opsx / rules SSOT） |
| W8 | 同步复验 | scripts/README 参数修正；SYNC_GUIDE 补映射表；sync.ps1 + check.ps1 复跑 |
| W9 | 验证留痕 | validate_config + check + 残留 Grep + MANIFEST/SPEC changelog |

## 五、优点→本地映射（沿用 v10.5.1，复用优先）

| 优点来源 | 本地落点 | 状态 |
| ------------------------- | ----------------------- | ------------ |
| Superpowers HARD-GATE/SDD | P0 + 五阶段 | integrated |
| GSD 阈值/制品 | CORE 阈值 + CONTEXT | integrated |
| OpenSpec OPSX | profile core + OPENSPEC | integrated |
| gstack 审查 | agents 5+7 | integrated |
| claude-mem R18 | 记忆柱 | integrated |
| codegraph R17 | `.mcp.json` 常驻 | integrated |
| cbm 架构/ADR/变更 | optional-dev L4 场景强制 | integrated |
| ECC 防互博 | MANIFEST conflicts | cherry_pick |
| RTK/caveman | hook/skill | integrated |
| Firecrawl+Exa | deep-research | integrated |
| sync 软链 | sync.ps1 v18 | integrated |

## 六、风险与回滚

| 风险 | 降级 |
| -------------------- | ---------------------------------- |
| CORE 瘦身丢规则 | 逐段对照迁移；GOVERNANCE 路由注册后验证 |
| 误删被引用文档 | 删前 Grep 全库引用；残留 >0 则先修引用 |
| 双源一侧失败 | 卡片已标来源；不编造 |
| 回滚 | git revert 对应 commit |

## 七、验收清单

- [x] 版本串全库对齐 v10.6.0
- [x] 旧 plan×2 + gsd-gaps + REPO_ANALYSIS 已删；COVERAGE 含并入内容
- [x] skills-INDEX 44 全量；2 个 SKILL.md frontmatter 修复
- [x] CORE ≤150 行；GOVERNANCE.md 已建并注册路由
- [x] hooks `_archive/` 正名；stub 已除名
- [x] usage-audit-v10.6.md 产出
- [x] OPENSPEC.md 职责表；sync 复跑 + validate 通过
