# v10.3 配置优化实施计划

> 日期: 2026-06-24 | 增量优化 v10.2.1 → v10.3 | 骨架不变,深化集成
> 语言: 文档/注释优先中文,代码保持原样
> For agentic workers: REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (推荐) 或 superpowers:executing-plans 按任务实施。步骤使用 checkbox (`- [ ]`) 跟踪。

**Goal:** 将 ~/.claude 配置从 v10.2.1 增量优化到 v10.3 — delta 刷新 stale repo 卡片 + codegraph F1 纠偏 + 7 编辑器同步扩展 + 一致性审计验证

**Architecture:** 五柱×五阶段×三横切 骨架不变;增量:codegraph_impact 启用 + sync.ps1 扩展 7 编辑器 + 验证 R16/R17/R18/loading_tiers 强制执行

**Tech Stack:** Markdown/YAML/PowerShell — 纯配置变更,无代码编译

**Spec:** 本计划即设计文档(访谈驱动,5 轮 20 问全部 resolved)

---

## 决策摘要(5 轮访谈,20 问,全部 resolved)

| # | 需求 | 决策分支 | 选定方案 |
|---|------|----------|----------|
| 1 | 保留文档/仓库优点 | 重新提取范围 | Delta refresh(stale 卡片 + 上游新版本) |
| 1 | 骨架 | 保留 or 重构 | 保留五柱×五阶段×三横切,深化集成 |
| 1 | docs 更新范围 | 全量 or 增量 | 刷新 SSOT + delta 卡片 + 新 ADR |
| 1 | 计划版本 | 继续 v10.2 or 新建 | 新建 v10.3,取代 v10.2 |
| 1 | 停止门 | 过度优化边界 | 16/16 + 无互博 + sync clean + 任务完成 |
| 2 | 强制加载 | 必要工具范围 | L0+L1 强制,其余按需(验证 settings.json) |
| 3 | 防互博 | 验证方法 | 静态审计:validate_config + grep + MANIFEST |
| 3 | 计划门控 | 执行前计划 | 完整计划文档 → 批准 → 逐任务执行 |
| 4 | 复用组件 | 工具复用 | validate_config.py + sync.ps1 + hooks + ADR 模板 |
| 5 | Token 效率 | 强制深度 | 验证 R17/R18 + loading_tiers 已强制 |
| 6 | 多源验证 | 来源选择 | 保持当前 3 源(Exa + Firecrawl + GitHub Releases) |
| 7 | 错误根因 | R16 强制 | 验证 CORE + grep bare except + stop-on-failure |
| 8 | 跨项目迭代 | 适配方式 | 全局 SSOT + 项目层覆盖(已 v10.2 模型) |
| 9 | 访谈 | 收敛 | 5 轮 20 问,已达成共识 |
| 10 | 编辑器同步 | 编辑器数量 | 7 编辑器(cursor/devin/qoder/qoder-cn/trae/trae-cn/codearts) |
| 10 | codegraph F1 | impact 工具 | .mcp.json env 启用 codegraph_impact |
| 10 | 插件冲突 | 验证方法 | grep 插件 skills vs 本地 skills + MANIFEST |

**用户补充约束:**
- 所有修改先计划后执行并验证,确保满足所有要求
- 除代码外,优先中文
- 同步编辑器:cursor, devin, qoder, qoder-cn, trae, trae-cn, codearts

---

## 文件结构

```
修改 (7):
  .mcp.json                                   — codegraph_impact env (F1 纠偏)
  scripts/sync.ps1                            — 扩展 7 编辑器 (add qoder-cn, trae-cn)
  MANIFEST.yaml                               — version→10.3 + sync editors list
  CLAUDE.md                                   — version→10.3
  SPEC.md                                     — version→10.3
  rules/CORE.md                               — F1 纠偏说明 (codegraph_impact env)
  docs/research/30-repo-deep-research-v10.md  — v10.3 变更日志

新增 (2):
  docs/ADR/2026-06-24-v10.3-optimization.md
  docs/superpowers/plans/2026-06-24-v10.3-optimization.md  (本文件)

delta 刷新 (N,Task 1 识别后确定):
  docs/research/repos/<stale>.md              — 刷新日期 < 2026-06-19 或上游新版本

验证 (4):
  python _validate_config.py                 — 16/16 通过
  grep plugin skills vs local skills         — 互博 report
  grep bare except:pass                      — R16 report
  sync.ps1 -DryRun                           — 7 编辑器 clean
```

---

## Task 1: 识别 stale repo 卡片

**Files:**
- Read: `docs/research/repos/*.md` (28 张卡片)

- [ ] **Step 1: 提取所有卡片的"刷新:"日期**

```powershell
# 用 Grep 工具(非 RunCommand)搜索所有卡片的刷新日期
# Grep pattern: "^> .*刷新: " path: docs/research/repos/ output_mode: content -n
```

预期:每张卡片头部有 `> 层: ... | 置信度: ... | 刷新: YYYY-MM-DD` 行。

- [ ] **Step 2: 比对日期,标记 stale**

判定标准(已确认):
- 刷新日期 < 2026-06-19 → stale
- 刷新日期 ≥ 2026-06-19 但上游有新 release → stale
- 刷新日期 = 2026-06-19 且无新 release → skip

已知 skip(2026-06-19 已刷新):obra-superpowers, open-gsd-gsd-core, fission-ai-openspec, garrytan-gstack, thedotmack-claude-mem, colbymchenry-codegraph, anthropics-claude-plugins-official, rtk-ai-rtk

- [ ] **Step 3: 输出 stale 卡片清单**

记录到本 Task 下方:
```
stale 卡片:
- [slug1] (原因: 日期 2026-06-XX < 2026-06-19)
- [slug2] (原因: 上游新版本 vX.Y.Z)
...
```

**验证:** 清单非空且每项有原因;skip 项有依据。

---

## Task 2: Delta 刷新 stale 卡片

**Files:**
- Modify: `docs/research/repos/<stale>.md` (Task 1 识别的卡片)

- [ ] **Step 1: 对每张 stale 卡片,用 3 源交叉验证刷新**

3 源(已确认,保持当前方法):
1. Exa(语义搜索)— 版本号、最新特性
2. Firecrawl(全页抓取)— GitHub Releases、README
3. GitHub Releases(官方)— 版本号、changelog

刷新内容:
- 版本号(最新稳定 + 最新预发布)
- 核心价值(如有新特性)
- 本地映射(如有变更)
- 吸收决策(如有调整)
- 互博检查(如有新冲突)
- 刷新日期 → 2026-06-24

- [ ] **Step 2: 更新卡片头部"刷新:"日期为 2026-06-24**

- [ ] **Step 3: 如有版本变更,同步 MANIFEST.yaml 对应 pillar/version**

仅当卡片版本号变化时才改 MANIFEST;无变化不动。

**验证:** 每张刷新卡片有 3 源证据;日期 = 2026-06-24;MANIFEST 版本一致(如有变更)。

---

## Task 3: 更新 SSOT v10.3 变更日志

**Files:**
- Modify: `docs/research/30-repo-deep-research-v10.md`

- [ ] **Step 1: 更新头部版本与日期**

```markdown
# 28 仓库深度调研报告 v10.3

> 日期: 2026-06-24 | 方法: Exa + Firecrawl/官方源 交叉验证 | 28 张卡片
> 状态: **已完成** | 运行配置: **v10.3** | 历史 v7/v8 → `archive/`
```

- [ ] **Step 2: 追加 v10.3 变更日志段**

在"v10 → v10.1 变更日志"前插入:

```markdown
## v10.3 变更日志(2026-06-24)

| 项 | v10.2.1 | v10.3 |
|----|---------|-------|
| codegraph F1 | impact 默认隐藏 | .mcp.json env 启用 codegraph_impact |
| sync.ps1 | 5 编辑器 | 7 编辑器(+qoder-cn, +trae-cn) |
| stale 卡片 | 28 张(部分 2026-06-19) | delta 刷新(Task 1-2) |
| 验证 | validate_config 16/16 | + plugin grep + R16 grep + sync --DryRun |
| 版本 | 10.2.1 | 10.3 |

骨架不变:五柱×五阶段×三横切。决策依据:5 轮访谈 20 问(见 plans/2026-06-24-v10.3-optimization.md)。
```

**验证:** Grep "v10.3" 在文件中出现;旧 v10.2.1 内容保留(增量,不覆盖)。

---

## Task 4: codegraph F1 纠偏

**Files:**
- Modify: `.mcp.json`
- Modify: `rules/CORE.md`

- [ ] **Step 1: .mcp.json 添加 codegraph_impact env**

在 codegraph MCP server 配置的 `env` 块中添加:

```json
"env": {
  "CODEGRAPH_MCP_TOOLS": "codegraph_explore,codegraph_node,codegraph_search,codegraph_callers,codegraph_impact"
}
```

理由(已确认):CORE.md R6 变更彻底性保障强制 codegraph_impact,但 v1.0.0 起 MCP 默认仅 4 工具。env 启用恢复 R6 铁律。

- [ ] **Step 2: rules/CORE.md 添加 F1 纠偏说明**

在 codegraph 相关段(R17 附近)追加:

```markdown
**codegraph F1 纠偏(v10.3)**:MCP 默认仅 4 工具(explore/node/search/callers);`codegraph_impact` 已通过 `.mcp.json` 的 `CODEGRAPH_MCP_TOOLS` env 显式启用,满足 R6 变更彻底性保障。如需 `codegraph_callees`/`codegraph_files`/`codegraph_status`,追加到同一 env。
```

- [ ] **Step 3: 验证 .mcp.json JSON 合法**

```powershell
python -c "import json; json.load(open('.mcp.json')); print('JSON valid')"
```
预期: `JSON valid`

**验证:** .mcp.json 合法;CORE.md 有 F1 说明;env 包含 codegraph_impact。

---

## Task 5: 扩展 sync.ps1 支持 7 编辑器

**Files:**
- Modify: `scripts/sync.ps1`

- [ ] **Step 1: 读取 sync.ps1 当前编辑器映射**

Read `scripts/sync.ps1`,定位 `$NATIVE_SKILLS_DIR_MAP` 或等价编辑器目标映射表。

- [ ] **Step 2: 添加 qoder-cn 和 trae-cn 目标**

在编辑器映射中追加(具体路径需 Read 后确认实际变量名):

```powershell
# qoder-cn — Qoder 中文版
"qoder-cn" = @{
    TargetDir = Join-Path $env:USERPROFILE ".qoder-cn/rules"
    SkillsNativeDir = Join-Path $env:USERPROFILE ".qoder-cn/skills-native"
    Format = "mdc"
}
# trae-cn — Trae 中文版
"trae-cn" = @{
    TargetDir = Join-Path $env:USERPROFILE ".trae-cn/rules"
    SkillsNativeDir = Join-Path $env:USERPROFILE ".trae-cn/skills-native"
    Format = "md"
}
```

注意:实际路径与变量名以 Read sync.ps1 后的现有结构为准;保持与现有 5 编辑器一致的映射格式。

- [ ] **Step 3: 更新 SYNC_GUIDE.md 编辑器列表**

在 `docs/SYNC_GUIDE.md` 的"目标编辑器"行追加 qoder-cn, trae-cn:

```markdown
- **目标编辑器**:cursor / devin / qoder / qoder-cn / trae / trae-cn / codearts
```

- [ ] **Step 4: DryRun 验证 7 编辑器**

```powershell
pwsh -File scripts/sync.ps1 -DryRun
```
预期:输出包含 7 个编辑器,无错误。

**验证:** -DryRun 输出 7 编辑器;SYNC_GUIDE 已更新;sync.ps1 语法合法。

---

## Task 6: 版本号对齐 v10.3

**Files:**
- Modify: `MANIFEST.yaml`
- Modify: `CLAUDE.md`
- Modify: `SPEC.md`

- [ ] **Step 1: MANIFEST.yaml 版本**

```yaml
version: "10.3"
updated: "2026-06-24"
```

- [ ] **Step 2: CLAUDE.md 版本**

头部 `**v10.2**` → `**v10.3**`

- [ ] **Step 3: SPEC.md 版本**

尾部 `版本:10.2.1` → `版本:10.3`;日期 `2026-06-19` → `2026-06-24`

- [ ] **Step 4: MANIFEST.yaml 添加 sync editors 声明**

在 `sync` 相关段(如 `cursor_guard` 或新增 `sync_targets`)追加:

```yaml
sync_targets:
  editors: [cursor, devin, qoder, qoder-cn, trae, trae-cn, codearts]
  method: symlink_first_copy_fallback
  script: scripts/sync.ps1
  modes: [default, -Skills, -All, -DryRun]
```

**验证:** Grep "10.3" 在三文件中出现;MANIFEST YAML 合法。

---

## Task 7: validate_config.py 16/16 + 修复 gaps

**Files:**
- Run: `_validate_config.py`
- Modify: 视 gap 而定

- [ ] **Step 1: 运行 validate_config**

```powershell
python _validate_config.py
```

- [ ] **Step 2: 记录通过/失败项**

```
通过: N/16
失败: [列出失败项]
```

- [ ] **Step 3: 修复每个失败项**

对每个失败项:
1. Read 相关文件
2. Edit 修复
3. Read 验证
4. 重跑 validate_config 确认该项通过

**禁止**(R5/R16):同一方案重试 >2 次;失败时 STOP 报告根因,不猜测掩盖。

- [ ] **Step 4: 确认 16/16 通过**

```powershell
python _validate_config.py
```
预期: `16/16 通过` 或等价成功输出。

**验证:** validate_config 输出 16/16;无失败项。

---

## Task 8: plugin-vs-local grep audit(互博 report)

**Files:**
- Grep: `~/.claude/skills/`, `catalog/skills/`, `settings.json`, `MANIFEST.yaml`

- [ ] **Step 1: 提取 15 启用插件的 skill 名称**

Read `settings.json`(或 MANIFEST plugins 段),列出 15 启用插件的 skill 列表。

- [ ] **Step 2: grep 本地 skills/ 和 catalog/skills/ 查同名**

```
Grep pattern: "^# .*skill name" 或 skill 名称
path: skills/, catalog/skills/
output_mode: content -n
```

- [ ] **Step 3: 检查 MANIFEST plugin_attribution 覆盖**

确认每个启用插件在 MANIFEST 有 `plugin_attribution` 或对应 concern 声明。

- [ ] **Step 4: 输出互博 report**

```
插件 vs 本地 skills 碰撞 report:
- [插件名] [skill名] vs [本地路径] — 决策: [本地覆盖 / 插件主 / 禁用插件]
...
无碰撞: [列出无碰撞插件]
```

已知决策(MANIFEST 已声明):
- superpowers:本地后加载覆盖
- understand-anything:插件 disabled,catalog 保留
- compound-engineering:禁用(与 gstack 审查重叠)

- [ ] **Step 5: 如有新碰撞,更新 MANIFEST excludes**

仅当发现未声明的碰撞时才追加 excludes;无新碰撞不动。

**验证:** report 完整;所有碰撞有决策;MANIFEST 一致。

---

## Task 9: R17/R18 enforcement 验证

**Files:**
- Grep: `rules/CORE.md`, `CLAUDE.md`, `hooks/`, `CLAUDE-ROUTER.mdc`

- [ ] **Step 1: 验证 R17(codegraph_explore 首选)**

```
Grep pattern: "codegraph_explore|R17"
path: rules/CORE.md, CLAUDE.md, CLAUDE-ROUTER.mdc
output_mode: content -n
```
预期:三文件均含 R17/codegraph_explore 引用。

- [ ] **Step 2: 验证 R18(claude-mem 先于重复 Read)**

```
Grep pattern: "claude-mem search|R18|记忆优先"
path: rules/CORE.md, CLAUDE.md
output_mode: content -n
```

- [ ] **Step 3: 验证 loading_tiers L0+L1 强制**

```
Grep pattern: "L0|L1|alwaysApply|loading_tier"
path: MANIFEST.yaml, settings.json, CLAUDE-ROUTER.mdc
output_mode: content -n
```
确认 L0(CLAUDE-ROUTER+CLAUDE+CORE)和 L1(using-superpowers+change-impact-analysis)为 alwaysApply/会话常驻。

- [ ] **Step 4: 如有 enforcement gap,修复**

仅当 R17/R18/loading_tiers 在某文件缺失时才追加;已有则不动。

**验证:** R17/R18 在 CORE+CLAUDE 出现;L0+L1 在 settings/MANIFEST 强制。

---

## Task 10: R16 bare-except grep + L0+L1 forced-load 验证

**Files:**
- Grep: `hooks/`, `skills/`, `catalog/skills/`

- [ ] **Step 1: grep bare except:pass**

```
Grep pattern: "except\s*:|except Exception|except\s+Exception\s*:.*pass"
path: hooks/, skills/, catalog/skills/
output_mode: content -n
type: py
```

- [ ] **Step 2: 对每个命中,检查是否 re-raise 或 log**

合法模式:
- `except SpecificError as e: raise` ✓
- `except Exception as e: logger.error(...); raise` ✓
- `except: pass` ✗(违反 R16)
- `except Exception: pass` ✗(违反 R16)

- [ ] **Step 3: 修复违规项**

对每个违规:
1. Read 文件
2. Edit:添加 `raise` 或 `logger.error(...)` + `raise`
3. Read 验证

- [ ] **Step 4: 验证 settings.json L0+L1 强制加载**

Read `settings.json`(或 Cursor 等价),确认:
- CLAUDE.md / CORE.md 为 alwaysApply
- using-superpowers / change-impact-analysis 为会话常驻或等价机制

**验证:** 无 bare except:pass;settings.json L0+L1 强制。

---

## Task 11: sync.ps1 -Skills 实际同步到 7 编辑器

**Files:**
- Run: `scripts/sync.ps1 -Skills`

- [ ] **Step 1: 执行实际同步**

```powershell
pwsh -File scripts/sync.ps1 -Skills
```

- [ ] **Step 2: 验证 7 编辑器目标存在**

```powershell
# 检查每个编辑器目标目录
Test-Path ~/.cursor/rules
Test-Path ~/.devin/rules
Test-Path ~/.qoder/rules
Test-Path ~/.qoder-cn/rules
Test-Path ~/.trae/rules
Test-Path ~/.trae-cn/rules
Test-Path ~/.codeartsdoer/rule
```
预期:全部 True(或等价软链接/副本存在)。

- [ ] **Step 3: 验证软链接有效(非悬空)**

抽查 2-3 个编辑器的 CLAUDE.md 软链接:

```powershell
Get-Item ~/.cursor/CLAUDE.md | Select-Object Target
Get-Item ~/.trae-cn/CLAUDE.md | Select-Object Target
```
预期:Target 指向 ~/.claude/CLAUDE.md。

**验证:** 7 编辑器目标存在;软链接非悬空;无冲突错误。

---

## Task 12: 最终验证(停止门)

**Files:**
- Run: 多项验证

- [ ] **Step 1: validate_config 16/16**

```powershell
python _validate_config.py
```
预期:16/16 通过。

- [ ] **Step 2: grep 残留引用**

```
Grep pattern: "v10.2.1|v10.2"
path: MANIFEST.yaml, CLAUDE.md, SPEC.md, rules/CORE.md
output_mode: content -n
```
预期:仅历史变更日志段保留 v10.2.1;运行配置段均为 v10.3。

- [ ] **Step 3: sync --DryRun clean**

```powershell
pwsh -File scripts/sync.ps1 -DryRun
```
预期:7 编辑器,无错误,无悬空链接。

- [ ] **Step 4: 确认所有 Task checkbox 完成**

回溯 Task 1-11 所有 `- [ ]` → `- [x]`。

- [ ] **Step 5: 停止门判定**

停止条件(已确认):
- ✓ validate_config 16/16 通过
- ✓ 无互博 grep findings(Task 8 report 清晰)
- ✓ sync --DryRun clean(7 编辑器)
- ✓ 所有 12 Task checkbox 完成

满足全部 → v10.3 优化完成。
任一未满足 → STOP,报告根因,不猜测掩盖(R16/R7)。

**验证:** 停止门 4 项全 ✓;记录最终状态到本文件末尾。

---

## 风险与回滚

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| sync.ps1 扩展破坏现有 5 编辑器 | 低 | 中 | -DryRun 先验证;git diff 审查 |
| .mcp.json env 导致 codegraph 启动失败 | 低 | 高 | JSON 合法性验证;如失败回退 env |
| stale 卡片刷新引入版本错误 | 中 | 低 | 3 源交叉验证;MANIFEST 版本一致性检查 |
| plugin grep 发现未声明碰撞 | 中 | 中 | 追加 MANIFEST excludes;不删除现有 |
| R16 修复改变 hook 行为 | 低 | 中 | 逐项 Read→Edit→Read;validate_config 兜底 |

**回滚:** 所有修改在 git 跟踪下;`git checkout -- <file>` 回退单文件;`git stash` 回退全部。

---

## 执行顺序依赖

```
Task 1 (识别 stale) → Task 2 (刷新 stale)
Task 2 → Task 3 (SSOT changelog)
Task 4 (codegraph F1) — 独立
Task 5 (sync.ps1) — 独立
Task 6 (版本号) — 独立,但建议在 Task 3 后
Task 4,5,6 → Task 7 (validate_config)
Task 8 (plugin audit) — 独立
Task 9 (R17/R18) — 独立
Task 10 (R16) — 独立
Task 7,8,9,10 → Task 11 (sync -Skills)
Task 11 → Task 12 (最终验证)
```

可并行:Task 4/5/6/8/9/10 互不依赖,可并行执行(如用 subagent-driven-development)。

---

## 完成状态

```
[ ] 计划已批准(用户签字)
[ ] Task 1-12 全部完成
[ ] 停止门 4 项全 ✓
[ ] v10.3 优化完成
```

---

> 版本:10.3 计划 | 日期:2026-06-24 | 五柱×五阶段×三横切 | 增量优化,骨架不变
