# v10.5 配置优化计划

> 日期: 2026-07-17 | 依据: Wave1 调研 + 访谈 Q1–Q5 | 状态: **批准执行（用户「执行」）**

## 访谈锁定

| #   | 结论                                                           |
| --- | -------------------------------------------------------------- |
| Q1  | 三波串行                                                       |
| Q2  | 分层 delta                                                     |
| Q3  | Cursor 工具纪律：soft_block + MCP P0(codegraph+cbm) + 常驻边界 |
| Q4  | 默认不重开 v10.1；上游版本仅文档跟踪（R14）                    |
| Q5  | **Understand-Anything removed**                                |

## 目标

在保持 `五柱 × 五阶段 × 三横切` 前提下：

1. 文档与卡片反映 v10.5 优点/漂移（已完成 Wave1）
2. 去除 UA，探索链收敛为 codegraph → cbm(L4) → Grep → Read
3. 加固 Cursor 工具纪律（提示→可 soft_block）
4. 纠偏 MCP 常驻面；软链 sync + Guard 重部署

## 非目标

- 不自动升 OpenSpec 1.6 / GSD 1.7 / codegraph 1.4 / cbm 0.9 / superpowers 6.1
- 不启用 ruflo；不装全量 ECC 插件
- 不引入新概念图谱替代 UA

## Wave3 变更清单（拓扑序）

### A. 文档/骨架口径

| 文件                                                    | 动作                                             |
| ------------------------------------------------------- | ------------------------------------------------ |
| `MANIFEST.yaml`                                         | version 10.5.0；L3 去掉 UA；UA concern → removed |
| `rules/CORE.md`                                         | 横切与 R17 去 UA                                 |
| `rules/MCP.md`                                          | UA → removed；探索链更新                         |
| `CLAUDE.md` / `SPEC.md`                                 | 摘要同步（最小 diff）                            |
| `skills-INDEX.md`                                       | understand-anything 迁出活跃索引                 |
| `docs/ADR/2026-07-17-v10.5-ua-removed.md`               | 新 ADR 废止 l3_on_demand                         |
| `docs/CURSOR_MCP_PROFILE.md` / `CURSOR_EDITOR_SETUP.md` | P0 含 cbm；纪律说明                              |

### B. UA 运行时清除

| 项                               | 动作                                            |
| -------------------------------- | ----------------------------------------------- |
| `skills/understand-anything/`    | 迁 `catalog/skills/understand-anything/`        |
| `plugins/installed_plugins.json` | `understand-anything@Lum1104` → disabled/false  |
| `settings.json`                  | 确认插件关闭                                    |
| Grep                             | 活跃路由 `/understand` 引用清零（文档历史除外） |

### C. Cursor 工具纪律

| 项                                               | 动作                                                           |
| ------------------------------------------------ | -------------------------------------------------------------- |
| `templates/cursor-guard/hooks/explore_router.py` | `enforce_mode: soft_block`（默认）；无 codegraph 时降级 nudge  |
| `templates/cursor-guard/guard-config.json`       | 增加 explore.enforce_mode                                      |
| `templates/cursor-guard/mcp-recommended.json`    | P0: codegraph + codebase-memory；P1: crawl/gh/context7/exa     |
| 回归                                             | `deploy-cursor-guard.ps1` + `test-cursor-guard-regression.ps1` |

### D. MCP 边界

| 项                              | 动作                                                                         |
| ------------------------------- | ---------------------------------------------------------------------------- |
| `.mcp.json`                     | 移除 chrome-devtools（回 optional-dev）；常驻 5：codegraph/crawl/git/fs/time |
| `mcp-configs/optional-dev.json` | 保持 chrome-devtools + cbm + exa + figma                                     |
| 文档                            | Claude Code 常驻 5；Cursor 手工启用 codegraph+cbm                            |

### E. 同步与验收

```
sync.ps1 -All
deploy-cursor-guard.ps1
python scripts/validate_config.py
# Guard 回归脚本若存在则跑
```

验收：

- [x] MANIFEST 无 UA active owner
- [x] Grep 活跃 understand 路由 = 0（catalog/ADR/research 除外）
- [x] `.mcp.json` 无 chrome-devtools
- [x] explore_router 支持 soft_block（烟测：Grep→deny；codegraph_seen 后→nudge）
- [x] validate 通过（ALL CHECKS PASSED）
- [x] Guard regression 25/25
- [x] sync.ps1 -All + deploy-cursor-guard 已执行

**验证日期**: 2026-07-17（二次验收）

## 最优推荐（本轮采纳）

| 主题 | 推荐                                                   |
| ---- | ------------------------------------------------------ |
| 探索 | codegraph 强制优先；cbm 仅架构/ADR/变更/跨服务         |
| 版本 | 钉扎现状；changelog 记「待评估」                       |
| 加载 | L0–L3 不变；P0 五技能不变                              |
| 互博 | MANIFEST conflicts 保留；删 UA 相关冲突项或标 obsolete |
| 同步 | rules/skills/agents 软链；MCP/plugins 不同步           |

## 风险

| 风险                 | 降级                         |
| -------------------- | ---------------------------- |
| Cursor deny API 无效 | 保留 nudge + 文档标注        |
| cbm 未 index         | 规则写清先 index；不阻断会话 |
| 迁 skill 断链        | INDEX + sync 同步更新        |
