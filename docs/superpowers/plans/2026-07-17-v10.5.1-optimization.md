# v10.5.1 配置优化计划

> 日期: 2026-07-17 | 依据: Wave1 分层 delta + 访谈 Q1–Q8 | 状态: **批准执行**

## 访谈锁定

| #   | 结论                                    |
| --- | --------------------------------------- |
| Q1  | 分层 delta；不推翻五柱×五阶段×三横切    |
| Q2  | 钉现状；上游仅文档「待评估」（R14）     |
| Q3  | Tier-1 双源；Tier-2 gh                  |
| Q4  | UA **removed**                          |
| Q5  | cbm 场景强制；Claude L4；Cursor P0      |
| Q6  | 多编辑器 sync；修 CONTEXT/CORE/MCP 过期 |
| Q7  | 三波串行                                |
| Q8  | **v10.5.1** patch                       |

前版：[`2026-07-17-v10.5-optimization.md`](2026-07-17-v10.5-optimization.md)（已验收，不改写正文）。

## 目标

1. 调研卡片与 SSOT 反映 v10.5.1 优点/漂移（Wave1 ✅）
2. 明确优点→本地映射；缺口仅纪律/文档补强
3. Wave3：版本口径 10.5.1 + cbm 场景强制 + sync 过期修复 + validate

## 非目标

- 不自动升 OpenSpec 1.6 / GSD 1.7 / codegraph 1.4 / cbm 0.9 / superpowers 6.1 / claude-mem 13.11
- 不重装 UA；不把 cbm 塞进 Claude 常驻 5
- 不启用 ruflo；不装全量 ECC；不新增编辑器

## 优点→本地映射（复用优先）

| 优点来源                  | 本地落点                | 状态         | Wave3 动作                   |
| ------------------------- | ----------------------- | ------------ | ---------------------------- |
| Superpowers HARD-GATE/SDD | P0 + 五阶段             | integrated   | 无                           |
| GSD 阈值/制品             | CORE 阈值 + CONTEXT     | integrated   | 无（EoS 不吸收）             |
| OpenSpec OPSX             | profile core + OPENSPEC | integrated   | 文档记 `/opsx:update` 待评估 |
| gstack 审查               | agents 5+7              | integrated   | 无                           |
| claude-mem R18            | 记忆柱                  | integrated   | 云同步不开                   |
| codegraph R17             | `.mcp.json` 常驻        | integrated   | 无                           |
| cbm 架构/ADR/变更         | optional-dev L4         | **纪律补强** | CORE/CONTEXT/MCP 场景强制    |
| ECC 防互博                | MANIFEST conflicts      | cherry_pick  | 无                           |
| RTK/caveman               | hook/skill              | integrated   | 文档 patch 可跟              |
| Firecrawl+Exa             | deep-research           | integrated   | 无                           |
| sync 软链                 | sync.ps1                | integrated   | `-All` 修过期                |

## 互博检查

对照 `MANIFEST.yaml` `module_resolver.conflicts`：Wave1 **无新增运行时冲突**。已知项保留（deer_flow↔workstream、task_master↔writing-plans、cbm search↔codegraph、UA 已 obsolete）。运行时冲突变更需另开访谈门控。

## 加载纪律（不变 + 补强）

- L0–L3 / P0 五技能：**不变**
- MCP Claude 常驻 5：**不变**
- cbm：架构/ADR/变更/跨服务 **必须** 调用；未调用 → `DONE_WITH_CONCERNS`；Claude 仍 merge 按需
- deep-research：Firecrawl + Exa 双源

## Wave3 变更清单（拓扑序）

| 文件                                      | 动作                                                      |
| ----------------------------------------- | --------------------------------------------------------- |
| `MANIFEST.yaml` / `SPEC.md` / `CLAUDE.md` | version → **10.5.1**（最小摘要）                          |
| `rules/CORE.md` / `CONTEXT.md` / `MCP.md` | cbm 场景强制表述                                          |
| `docs/research/*`                         | 已完成；必要时 `REPO_ANALYSIS.md` / `docs/README.md` 指针 |
| `scripts/sync.ps1 -All`                   | 消除 CONTEXT/CORE/MCP 过期                                |
| `validate_config.py`                      | 验收                                                      |

## 验收

- [x] Tier-1/2 卡含 v10.5.1 delta；SSOT/COVERAGE 已更新
- [x] 本计划 + design-v10.5.1 存在
- [x] MANIFEST/SPEC = 10.5.1；cbm 场景强制已写清
- [x] validate 通过；sync 后 rules 无 CONTEXT/CORE/MCP 过期
- [x] `.mcp.json` 常驻仍 5；UA 仍 removed

**验证日期**: 2026-07-17（Wave3）

## 风险

| 风险          | 降级                           |
| ------------- | ------------------------------ |
| 双源一侧失败  | 卡片已标来源；不编造           |
| sync 联接失败 | 暴露错误；Cursor 优先          |
| 误升依赖      | Wave3 禁止改 optional-dev 钉扎 |
