{
  "toolset": "dev",
  "description": "开发常驻：codegraph + crawl + fetch + git + code-review-graph（与 .mcp.json 同步）",
  "servers": ["codegraph", "crawl", "fetch", "git", "code-review-graph"],
  "note": "chrome-devtools 已移至 optional-dev.json 按需启用；code-review-graph v10.14 引入为审查/验证专用层"
}
