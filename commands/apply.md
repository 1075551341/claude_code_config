# /apply — 应用规格变更

基于已批准的规格提案，执行实现。

## 流程

1. 读取 `specs/CHANGE_NAME/tasks.md`
2. 按顺序执行任务清单
3. 每完成一项，标记 `[x]`
4. 全部完成后运行 `/verify`

## 执行原则

- 严格按 tasks.md 顺序执行
- 每个任务独立验证
- 偏离计划时更新 design.md 并通知用户
- 不超出提案范围
