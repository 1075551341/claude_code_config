---
description: 战略上下文压缩与模式提取（⑤学习阶段）
---

# /compact — 战略上下文压缩

在逻辑断点主动压缩上下文，保持上下文健康度 <70%（达 90% 强制压缩）。

**正文 SSOT**：Read `skills/memory-compression/SKILL.md`（触发条件 + 压缩策略 + 摘要输出格式 + claude-mem 职责边界）。

触发：子目标完成时 / 上下文 ≥70% 择机、≥90% 强制 / 长任务阶段性输出后 / 工作流阶段切换时。

> 平台差异：Claude Code 用 `/compact`；Cursor 用 `/summarize` 或「压缩上下文」（见 rules/CURSOR-EDITOR）。
