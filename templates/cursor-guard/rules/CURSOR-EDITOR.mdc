---
description: Cursor 编辑器专有约定（非 sync 规则，与 Claude Code hooks 低耦合）
alwaysApply: true
---

# Cursor 编辑器专有

> 五阶段/铁律见 sync 的 CORE.md、CLAUDE.md。本文仅 Cursor Agent 行为。

## 探索优先级（R17 Cursor 版 · 严格三级禁止跳级）

```
1. 结构/怎么运作 → codegraph_explore（禁止直接 Grep/Read）
2. 精准上下文/变更影响/风险/审查/PR → CRG（有 `.code-review-graph/graph.db`）；无图则 SessionStart/PreToolUse ensure，仍无则 deny（禁止 Grep 当主路径）
3. codebase-memory：已禁用 — 勿调用
4. 为什么 / 约定 / 偏好 → claude-mem（勿塞入 codegraph）
```

- 结构、调用链、「怎么运作」：必须先 MCP `codegraph_explore`
- 影响面/风险/审查/开 PR：有 CRG 图时 `get_minimal_context` + `get_impact_radius` + `detect_changes` / `get_review_context`
- Grep 仅双图就绪后作残留核对（非跳级捷径）；无图 **deny**
- 禁止未探索就大范围 Read 扫目录
- chrome-devtools / postgres 默认关；需要时**中断请用户手动启用**（不改 Plugin 面板、不自动 merge MCP）
- 工具优先级：内置浏览器（UI 核验）> playwright plugin（E2E）> chrome-devtools（调试）
- codegraph 索引由 MCP server 自动同步（v1.5 原生监听；v11 起 Guard 不再挂 kg sync hook；cbm 已关）

## 编辑与引用

- 代码引用：` ```startLine:endLine:path ````
- 优先 `StrReplace` 小 diff；避免无必要整文件重写
- Ask/Plan 模式：不写盘、不跑破坏性 Shell

## 上下文与压缩（Cursor ≠ Claude Code）

| 目标                             | Cursor 操作                                                               |
| -------------------------------- | ------------------------------------------------------------------------- |
| **降低上下文环 / 压缩当前会话**  | Agent 输入框发送 **`/summarize`** 或 **「压缩上下文」**（二者等效）       |
| **获取结构化会话摘要（不压缩）** | 发送 **「提取上下文」** → Agent 输出摘要 → Guard 写入 `session-digest.md` |
| CLI 终端 Agent                   | **`/compress`**                                                           |
| Claude Code                      | **`/compact`**（Cursor 中无效）                                           |

- 无 Command Palette 的 Compact 命令；点击输入框旁**上下文环**仅查看占用，不触发压缩
- 「压缩上下文」与 `/summarize` 同为压缩意图；Guard 不拦截，由 Cursor 原生压缩处理
- 「提取上下文」才触发结构化摘要制品；需要**先摘要再压缩**时：提取 → `/summarize`

## Stop 门控

- Cursor `stop` **不能** `permission: deny`（仅 shell/MCP 能 deny）
- 完成验证用短 `followup_message` 续轮（`loop_limit` 对齐 `max_blocks`）
- **计划未批准 / CreatePlan / 本轮零编辑 / 无 session id → 禁止 followup**（避免计划卡片等待时自动跑 R20）
- **非简单双审**：修改→验证→审查循环最多 3 轮（对照原始要求审全部修改；禁止只连审不改）
- 每个文件首次编辑后注入五维迷你验收；核对范围 = 该文件 + blast-radius 全部相关项

## 工具路由

- 库文档：Context7 plugin
- GitHub：`gh` CLI（github plugin 默认关）
- 跨会话：Guard 摘要制品 + 新对话 `@session-digest.md`
- E2E：内置浏览器（UI）+ playwright plugin（脚本）
- 调试/DB：中断请用户手动启用 chrome-devtools / postgres
