---
name: incremental-arch
description: 增量架构同步变体（catalog）。权威不在全局 skills。
---

# incremental-arch（catalog 变体）

基于 AST 感知分块和 Merkle DAG 的高效上下文更新。本目录曾是指向本机 `~/.cc-switch/skills/` 的联接；仓库内保留占位正文，避免 Linux/云端断链。

不参与全局加载。
