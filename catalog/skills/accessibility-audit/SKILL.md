---
name: accessibility-audit
description: 无障碍审计变体（catalog）。权威实现不在全局 skills；按需复制到项目 .claude/skills/。
---

# accessibility-audit（catalog 变体）

检查 WCAG 合规性并生成修复建议。本目录曾是指向本机 `~/.cc-switch/skills/` 的联接；仓库内保留占位正文，避免 Linux/云端断链。

不参与全局加载。复制：`python scripts/migrate-from-legacy.py --skill accessibility-audit`（若脚本可用）或手工拷到项目 `.claude/skills/`。
